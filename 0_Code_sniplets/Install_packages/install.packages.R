# install packages

install.packages("your_package_name") # for the installation of the package

library(your_package_name)    # to load the packages content and use it

# e.g.
# install.packages("superheat")