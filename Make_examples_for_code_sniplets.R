col for correlation


abs()


which()

names()

lm(
  
  matlines(
    
    
    subset(Wage, select = - c(logwage))
    
    featurePlot
    
    
    assign(WT_Exon_names[exon_count], for_mean)
    
    ifelse(test_expression, x, y)